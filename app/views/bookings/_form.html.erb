<%= simple_form_for[@booking, @kitten] do |f| %>
  <%= f.input :date, as: :date, start_year: Date.today.year - 90,
                              end_year: Date.today.year - 12, discard_day: true,
                              order: [:month, :year] %>
  <%= f.input :accepts, as: :boolean, checked_value: true, unchecked_value: false %>
  <%= f.association :user %>
  <%= f.submit %>
<% end %>
